import axios from 'axios'; const api=axios.create({ baseURL:'http://localhost:8000/api' }); export default api; export type Employee={id:number;first_name:string;last_name:string;company:string;email:string;is_active:boolean;full_name:string}; export type Shift={code:string;name:string;start_hhmm:string;end_hhmm:string;overnight:boolean}; export type ScheduleEntry={id:number;employee_id:number;date:string;shift_code:string|null;leave_type:string|null};
